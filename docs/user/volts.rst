.. _volts:

volts:
======

**calculate volts using ohms law:**::

        >>> from ohmslaw import Ohms
        >>> 
        >>> o = Ohms()
        >>> results = o.current(12, 4)
        >>> 
        >>> print(results)
        3.0

