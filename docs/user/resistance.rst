.. _resistance:

Resistance:
===========

**calculate resistance in ohms:**::

        >>> from ohmslaw import Ohms
        >>> 
        >>> o = Ohms()
        >>> results = o.resistance(48, 4)
        >>> 
        >>> print(results)
        12.0
        >>> 

