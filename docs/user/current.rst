.. _current:

Current:
========

**returns the electrical current:**::

        >>> o = Ohms()
        >>> results = o.current(V=12, R=4)
        >>> 
        >>> print(results)
        3.0
        >>> 
